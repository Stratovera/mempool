#!/bin/bash
set -euo pipefail

NETWORKS="${MEMPOOL_NETWORKS}"

for network in ${NETWORKS//,/ }; do
    dir="${MEMPOOL_BASE_DIR}/${network}"
    if [[ -d "$dir" ]]; then
        echo "Starting ${network}"
        (cd "$dir" && docker compose up -d)
    fi
done
